<?xml version="1.0"?>

<!-- Copyright (c) 2008 by ParaRede. All Rights Reserved. -->

<project name="alfresco-container-security" default="release" basedir=".">
	
	<property name="src.dir" value="src/java" />
	<property name="build.dir" value="build" />	
	<property name="dist.dir" value="dist" />
	<property name="lib.dir" value="lib" />
		
	<property name="jar.version" value="1.0" />
	<property name="jar.name" value="${ant.project.name}-${jar.version}" />
	
	<target name="prepare">
		<mkdir dir="${build.dir}" />
		<mkdir dir="${dist.dir}" />
	</target>

	<target name="clean">
		<delete dir="${build.dir}" />
	</target>
	
	<path id="class.path">
		<fileset dir="${lib.dir}" includes="*.jar" />
		<pathelement path="${java.class.path}/" />
	</path>
	
	<target name="compile" depends="clean,prepare">
		<javac srcdir="${src.dir}" destdir="${build.dir}" debug="on" deprecation="off" optimize="on" target="1.5" source="1.5">
			<classpath refid="class.path" />
		</javac>
	</target>
	
	<target name="release" depends="compile">
		<jar destfile="${dist.dir}/${jar.name}.jar" basedir="${build.dir}" includes="**/*" />
	</target>
</project>